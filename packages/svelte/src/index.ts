// Minimal stub so the package is valid.
// You can later implement actual Svelte components wrapping @ajmal_n/flashify-core.

export function createFlashifyStore() {
  // TODO: expose @ajmal_n/flashify-core store as a Svelte store.
  return {};
}
