// Minimal stub so the package is valid.
// You can later implement actual Svelte components wrapping @flashify/core.

export function createFlashifyStore() {
  // TODO: expose @flashify/core store as a Svelte store.
  return {};
}
