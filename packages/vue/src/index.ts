// Minimal stub so the package is valid.
// You can later implement actual Vue composables/components wrapping @flashify/core.

export function createFlashifyPlugin() {
  // TODO: implement Vue plugin that wires @flashify/core into app provide/inject.
  return {
    install() {
      // no-op stub
    },
  };
}
