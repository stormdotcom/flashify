# Scripts

This directory is reserved for helper scripts (build, release, etc.) for the Flashify monorepo.

You can add Node/TypeScript scripts here later, for example:
- `build-all.ts`
- `release.ts`
